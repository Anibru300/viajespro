Kimi, necesito que revises y corrijas dos problemas:

1. En app.js, la función deleteVendor está causando "Cannot read properties of undefined (reading 'target')". Por favor, corrige esa función para que use la Cloud Function deleteVendor correctamente.

2. Las llamadas a las Cloud Functions createVendor y deleteVendor fallan con error 401 (Debes iniciar sesión) a pesar de que el admin está autenticado. Por favor, verifica que la instancia de Functions esté correctamente inicializada con la app de Firebase que tiene la sesión activa. Si es necesario, modifica la inicialización de Functions en app.js para usar la app exportada de firebase-config.js y especifica la región 'us-central1'.

Aquí están las funciones actuales en app.js para referencia:

// Función deleteVendor actual (con error)
async function deleteVendor(username) {
    if (!confirm(`¿Eliminar al vendedor ${username}?`)) return;
    try {
        await db.delete('vendedores', username);
        showToast('✅ Vendedor eliminado', 'success');
        loadVendorsList();
    } catch (error) {
        showToast('Error al eliminar', 'error');
    }
}

// Necesita ser reemplazada por una que llame a la Cloud Function deleteVendor.

// Inicialización de Functions actual (probablemente incorrecta)
// (dime cómo está actualmente si puedes verla)

Por favor, corrige estos archivos y luego probaremos nuevamente.